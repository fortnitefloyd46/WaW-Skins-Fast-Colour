# install_requirements.bat:
#   1) Finds or installs Python (winget or you install from python.org)
#   2) Creates .venv in this folder
#   3) pip installs packages below into .venv
#   4) Installs DirectXTex texconv via winget (DDS/PNG for COLOR — not a pip package)
#
# run_iwd_recolor_gui.bat always uses .venv\Scripts\python.exe
#
# Manual:  python -m venv .venv
#          .venv\Scripts\python.exe -m pip install -r requirements.txt

Pillow>=10.0.0
numpy>=1.24.0
pygame-ce>=2.5.0
